rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite acceso público temporalmente para testing
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}