rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permite acceso de lectura/escritura a todos los usuarios autenticados
    match /historial-odontologia/{docId}/adjuntos/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}